{
  "name": "simple-task-management-api",
  "module": "src/index.js",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "format": "prettier --write .",
    "lint:typecheck": "biome check --write --unsafe . && tsc --noEmit && cd convex && tsc --noEmit"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "@biomejs/biome": "latest",
    "@ianvs/prettier-plugin-sort-imports": "latest",
    "bun-types": "latest",
    "prettier": "latest"
  }
}
